// should render a container with 'Total savings'
// should show N/A with empty basket

// should show no discount with items in the basket
// oranges, oranges, coke, beans

// should show beans discount
// beans, beans, beans, orange

// should show coke discount
// coke, oranges, coke

// should show correct discount if applied, and then item(s) are removed
